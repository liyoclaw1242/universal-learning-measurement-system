{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the pdf-reader window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
